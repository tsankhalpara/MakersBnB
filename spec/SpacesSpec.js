describe("Spaces", function() {
  var spaces;

  beforeEach(function() {
    spaces = new Spaces();
  });

  describe("name", function() {
    it("has a name", function() {
      expect(spaces.name).toEqual(name);
    });
  });


});
